// Domain needs to be passed in as an array
// For simple bar charts (for example), we can simply map the index of each value in an our data array

var dataDomain = [];
var xDomain = data.map(function(i){return dataDomain.push(i); }); // Pass this variable to our x-axis domain as appropriate